services:
  - type: web
    name: tableproof-api
    runtime: docker
    rootDir: backend
    plan: free
    envVars:
      - key: FRONTEND_ORIGIN
        sync: false
      - key: JWT_SECRET
        generateValue: true
